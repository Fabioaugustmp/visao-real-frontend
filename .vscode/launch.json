{
    "version": "0.2.0",
    "configurations": [
    {
        "name": "Debug Angular in Chrome",
        "type": "chrome",
        "request": "launch",
        "url": "http://localhost:4200", // Ensure this matches your Angular app's port
        "webRoot": "${workspaceFolder}",
        "sourceMaps": true,
        "trace": true
    }
    ]
}